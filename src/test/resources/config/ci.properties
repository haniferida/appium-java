# GitHub Actions — Android emulator (API 30 / Android 11)
appium.server.url=http://127.0.0.1:4723
platform.name=Android
device.name=emulator-5554
platform.version=11
automation.name=UiAutomator2
# Wait for catalog screen, not SplashActivity (app.wait.activity=* returns too early on CI)
app.wait.activity=com.saucelabs.mydemoapp.android.view.activities.MainActivity
auto.grant.permissions=true
app.wait.duration=60000
app.ready.timeout.seconds=120
explicit.wait.seconds=30
reuse.session=true
no.reset=true

# GitHub-hosted emulators are slow; default 20s adb timeouts are too tight for UiAutomator2 server install
adb.exec.timeout=120000
uiautomator2.server.install.timeout=120000
uiautomator2.server.launch.timeout=120000

sample.app=mda-native
